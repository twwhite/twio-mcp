[project]
name = "twio-mcp"
version = "0.1.0"
description = "MCP hub for lazy-loading an arbitrary list of commands for LLM tool calling"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.27.0",
    "sympy>=1.14.0",
]

[project.scripts]
twio-mcp = "twio_mcp.server:main"
